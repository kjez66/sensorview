//go:build usb

package cmd

// usbSupportNote describes USB panel availability in the root command help.
const usbSupportNote = `AX206-based USB panels (480x320, RGB565) are also supported, inherited from the
upstream project this is derived from - see 'sensorview device select'.`
